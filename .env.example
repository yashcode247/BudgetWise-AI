# PIN PLAN - Environment variables
# Copy to .env and fill in your key. Never commit .env or hardcode keys.
# The app uses load_dotenv() to load these; do not hardcode DEDALUS_API_KEY in code.

DEDALUS_API_KEY=your_key_here
